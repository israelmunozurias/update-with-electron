{
  "name": "update-with-electron",
  "version": "1.0.2",
  "description": "Actualizaciones automaticas en electron",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/israelmunozurias/update-with-electron"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --linux --win --publish never",
    "deploy": "electron-builder build --linux --win --publish always"
  },
  "author": "israel",
  "license": "ISC",
  "devDependencies": {
    "electron": "^5.0.1",
    "electron-builder": "^20.40.2"
  },
  "dependencies": {
    "electron-updater": "^4.0.6"
  }
}
